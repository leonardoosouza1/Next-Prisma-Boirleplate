import prisma from '../../../src/lib/prisma'

//TODO: continue this feature

const handleTheme = async (req, res) => {
    // const [theme, id] = [req.body.theme, req.query.id]

    // if (req.method === 'PATCH') {
    //     const result = await prisma.themes.patch({
    //         where: id,
    //         data: JSON.stringify(theme)
    //     })
    //     res.json(result)
    // }

    // if (req.method === 'DELETE') {
    //     const result = await prisma.themes.delete({
    //         where: id
    //     })
    //     res.json(result)
    // }

    // if (req.method === 'GET') {
    //     const result = await prisma.themes.findUnique({
    //         where: id
    //     })
    //     res.json(result)
    // }
}