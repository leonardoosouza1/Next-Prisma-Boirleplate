# Boirleplate using NextJS and Prisma

## Docs

### create app
`https://vercel.com/guides/nextjs-prisma-postgres`


## Utils commands

### Clear all dockers
`docker stop $(docker ps -a -q) && docker rm $(docker ps -a -q)`

### Open bash
`docker exec -t local.development.com bash`
